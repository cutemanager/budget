# 개인 가계부 웹사이트 계획서

## 1. 프로젝트 목표

개인 가계부 웹사이트의 핵심 목표는 아래 3가지입니다.

- 수입/지출을 빠르게 기록할 수 있어야 한다.
- 이번 달 소비 흐름과 남은 예산을 한눈에 볼 수 있어야 한다.
- 복잡한 회계 기능보다 개인 사용에 맞는 직관적인 경험을 제공해야 한다.

## 2. 제품 방향

`command.md` 기준으로 가장 중요한 기능은 `빠른 입력`입니다. 따라서 전체 서비스는 아래 원칙으로 설계합니다.

- 페이지 수는 최소화한다.
- 모바일에서도 쉽게 입력할 수 있게 한다.
- 월별 요약과 예산 경고를 중심으로 보여준다.
- 초기 데이터 저장은 JSON 파일 방식으로 단순하게 시작한다.
- 처음 버전은 혼자 쓰는 개인용 MVP에 집중한다.

## 3. MVP 범위

처음 출시할 버전에는 아래 기능을 포함합니다.

### 필수 기능

- 초기 단일 사용자 기준 데이터 관리
- 거래내역 입력
- 수입/지출 카테고리 관리
- 월별 거래내역 조회
- 거래 수정 및 삭제
- 월 전체 예산 설정
- 카테고리별 예산 설정
- 대시보드 통계
- 예산 사용률 경고

### 출시 후 추가하기 좋은 기능

- 자주 쓰는 항목 즐겨찾기
- 반복 지출 자동 등록
- CSV 내보내기
- 캘린더 보기
- 월간 리포트 다운로드
- 다크 모드
- 여러 통장/지갑 자산 관리

## 4. 추천 페이지 구성

페이지는 아래 4개를 기본으로 잡습니다.

### 1) 대시보드

보여줄 항목:

- 이번 달 총수입
- 이번 달 총지출
- 이번 달 잔액
- 카테고리별 지출 비율
- 예산 초과 또는 주의 카테고리
- 최근 거래내역 5~10개

### 2) 빠른 입력 페이지

입력 항목:

- 날짜
- 수입/지출 구분
- 금액
- 카테고리
- 결제수단
- 메모

입력 UX 포인트:

- 기본 날짜는 오늘로 설정
- 금액 입력창을 가장 먼저 강조
- 최근 사용한 카테고리를 우선 노출
- 저장 후 폼 초기화 또는 연속 입력 가능
- 모바일 숫자 키패드에 최적화

### 3) 내역 조회 페이지

핵심 기능:

- 월별 조회
- 카테고리 필터
- 키워드 검색
- 수입/지출 타입 필터
- 수정 / 삭제

### 4) 예산 관리 페이지

핵심 기능:

- 월 전체 예산 설정
- 카테고리별 예산 설정
- 사용률 표시
- 80% 이상 주의 표시
- 100% 이상 초과 표시

## 5. 핵심 기능 상세

### A. 빠른 입력 기능

이 서비스에서 가장 중요한 기능입니다. 사용자는 복잡한 폼보다 아래 흐름으로 빠르게 기록할 수 있어야 합니다.

예시 입력 흐름:

- `15000 / 식비 / 카드 / 점심`

구현 포인트:

- 수입/지출 토글은 상단 고정
- 금액 입력 후 카테고리 선택으로 자연스럽게 이동
- 카테고리는 드롭다운과 자주 쓰는 버튼을 함께 제공
- 저장 버튼은 하나만 두고 단순하게 유지
- 저장 후 "같은 날짜로 계속 입력" 흐름 지원

### B. 월별 대시보드

상단 핵심 카드:

- 총수입
- 총지출
- 남은 금액

중간 시각화:

- 카테고리별 지출 원형 차트
- 일별 지출 추이 그래프

하단 정보:

- 예산 경고 박스
- 카테고리별 통계
- 최근 거래내역

카테고리 통계 최소 항목:

- 카테고리명
- 이번 달 사용금액
- 전체 지출 대비 비율

### C. 예산 경고 기능

단순하고 직관적으로 시작합니다.

- 80% 이상: 주의
- 100% 이상: 초과

예시:

- 식비 예산 400,000원 중 360,000원 사용 -> 90% 사용
- 쇼핑 예산 150,000원 중 180,000원 사용 -> 30,000원 초과

## 6. 사용자 입장에서 넣으면 좋은 추가 기능 제안

`command.md`의 범위를 유지하면서도 개인 가계부에 잘 맞는 보강 기능입니다.

- 최근 사용 카테고리 자동 추천
- 마지막 결제수단 자동 선택
- 메모 없이도 저장 가능한 초간단 입력
- 거래 삭제 전 확인 모달
- 월 바꾸기 쉬운 상단 월 선택기
- 예산 초과 카테고리만 모아 보는 필터
- 거래가 없는 날도 확인 가능한 월간 소비 추이

## 7. 데이터 구조 초안

처음 버전은 아래 구조를 각각 JSON 파일로 저장해도 충분합니다.
이후 필요해지면 같은 구조를 기준으로 DB 테이블로 옮길 수 있습니다.

### `users`

- `id`
- `email`
- `password_hash`
- `created_at`

### `categories`

- `id`
- `user_id`
- `name`
- `type` (`income` / `expense`)
- `color`
- `created_at`

### `transactions`

- `id`
- `user_id`
- `category_id`
- `type` (`income` / `expense`)
- `amount`
- `payment_method`
- `memo`
- `transaction_date`
- `created_at`

### `budgets`

- `id`
- `user_id`
- `month`
- `category_id` nullable
- `amount`
- `created_at`

설명:

- `category_id`가 있으면 카테고리별 예산
- `category_id`가 없으면 월 전체 예산

## 8. 대시보드 계산 로직

### 이번 달 총수입

- 해당 월의 `income` 합계

### 이번 달 총지출

- 해당 월의 `expense` 합계

### 이번 달 잔액

- `총수입 - 총지출`

### 카테고리 비율

- `각 카테고리 지출 / 총지출 * 100`

### 예산 사용률

- `현재 사용금액 / 예산 * 100`

## 9. 기본 카테고리 제안

### 지출 카테고리

- 식비
- 교통
- 카페/간식
- 쇼핑
- 주거
- 공과금
- 통신
- 의료
- 취미
- 기타

### 수입 카테고리

- 월급
- 부수입
- 용돈
- 기타수입

## 10. 기술 스택 제안

처음 만드는 개인 프로젝트 기준 추천 조합:

- 프론트엔드: Next.js + React
- 스타일: Tailwind CSS
- 차트: Recharts
- 서버 로직: Next.js Route Handlers
- 데이터 저장: JSON 파일 + Node.js `fs/promises`

이 조합을 추천하는 이유:

- 구조가 단순해서 바로 구현 가능
- 별도 데이터베이스 없이 빠르게 시작 가능
- 추후 DB로 옮기기 쉬움
- 배포가 쉬움
- MVP를 빠르게 만들기 좋음

대안:

- 추후 Supabase로 이전
- 추후 Next.js + Prisma + PostgreSQL로 확장

## 11. 개발 우선순위

### 1단계: 프로젝트 기반 만들기

- Next.js 프로젝트 세팅
- JSON 데이터 파일 구조 생성
- 기본 카테고리 시드 데이터 준비

### 2단계: 입력 기능 완성

- 카테고리 CRUD
- 거래내역 입력 폼
- 빠른 입력 UX 반영

### 3단계: 내역 관리 기능 추가

- 월별 내역 조회
- 검색 및 필터
- 수정 / 삭제

### 4단계: 대시보드 구축

- 월별 합계 계산
- 카테고리 차트 연결
- 최근 거래내역 표시

### 5단계: 예산 기능 완성

- 월 예산 설정
- 카테고리별 예산 설정
- 예산 경고 표시

## 12. UI 구성 초안

### 대시보드

상단:

- 총수입 카드
- 총지출 카드
- 잔액 카드

중간:

- 카테고리별 지출 차트
- 일별 지출 그래프

하단:

- 예산 경고 리스트
- 최근 거래내역

### 빠른 입력 페이지

위에서 아래 순서:

- 수입/지출 토글
- 금액 입력
- 카테고리 선택
- 날짜
- 결제수단
- 메모
- 저장 버튼

## 13. 초반 제외 범위

처음부터 넣지 않아도 되는 기능:

- 은행 계좌 자동 연동
- 카드 내역 자동 수집
- 가족 공유 기능
- 투자 자산 관리
- 환율/다국어 지원
- 정교한 통계 리포트

## 14. 최종 제안

이 프로젝트는 `빠른 입력 -> 월별 확인 -> 예산 경고` 흐름만 잘 만들어도 충분히 가치가 있습니다.

처음 버전에서는 아래 3가지를 가장 잘 만드는 것이 중요합니다.

- 몇 초 안에 거래를 입력할 수 있는가
- 이번 달에 얼마를 썼는지 바로 보이는가
- 예산을 초과하기 전에 경고를 받을 수 있는가

이 기준으로 MVP를 만들고, 이후 반복 지출이나 내보내기 같은 확장 기능을 붙이는 방식이 가장 안정적입니다.
